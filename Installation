# Doctor Appointment Chatbot Project

## Overview

This project is a Doctor Appointment Chatbot designed to help users book appointments with doctors. The chatbot is built using Python and leverages various libraries to provide a seamless user experience.

## Features

- Book appointments with doctors
- View available time slots
- Cancel appointments
- Receive reminders for upcoming appointments

## Installation

To get started with the project, follow these steps:

1. Clone the repository:
    ```sh
    git clone https://github.com/ESWARABALAJIV/Doctor_Appointment_Chatbot_Project.git
    ```

2. Navigate to the project directory:
    ```sh
    cd Doctor_Appointment_Chatbot_Project
    ```

3. Install the required dependencies:
    ```sh
    pip install -r requirements.txt
    ```

## Usage

To run the chatbot, execute the following command:
```sh
python chatbot.py
